[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dotfiles-installer"
description = "Current-user development environment installer"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "lincl @ https://github.com/ChristfriedBalizou/lincl/archive/07cfd3d349e6d9b2ede3bd6dbe634e9796b3719d.tar.gz",
]
dynamic = ["version"]

[project.scripts]
dotfiles-install = "dotfiles_installer.cli:main"
src-jump = "dotfiles_installer.src_jump:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "dotfiles_installer.__version__"}

[tool.black]
line-length = 79
target-version = ["py39"]
